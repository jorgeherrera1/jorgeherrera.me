---
import type { Article } from '../../utils/blog.ts';
import { formatDate, formatDateForAttribute, calculateReadingTime } from '../../utils/blog.ts';
import TagList from '../TagList.astro';

export interface Props {
  article: Article;
}

const { article } = Astro.props;
const readingTime = calculateReadingTime(article.body);
---

<article class="mb-6 p-4 border rounded-lg transition-colors">
  <header class="mb-3">
    <h2 class="text-xl font-semibold mb-2">
      <a 
        href={`/blog/${article.id}`}
        class="no-underline hover:underline"
      >
        {article.data.title}
      </a>
    </h2>
    <div class="flex items-center gap-3 text-sm">
      <time datetime={formatDateForAttribute(article.data.date)}>
        {formatDate(article.data.date)}
      </time>
      <span>•</span>
      <span>{readingTime} min read</span>
    </div>
  </header>
  
  <div class="leading-relaxed article-preview">
    {article.body.slice(0, 300)}
  </div>
  
  <TagList tags={article.data.tags || []} className="mt-3 pt-3 border-t" />
</article>